@{
    ViewData["Title"] = "Company";
}
@model IEnumerable<SubdivisionViewModel>

<div class="text-center">
    <h1>Список подразделений</h1>
    <a asp-controller="Home" asp-action="Index" class="btn btn-info btn-sm" type="button">Назад</a>
    <table class="table">
        <tr>
            <th>
                Название подразделения
            </th>
            <th>
            </th>
        </tr>
        @foreach (SubdivisionViewModel emp in Model)
        {
            <tr>
                <td>@emp.Name</td>
                <td><a asp-action="Edit" asp-route-subdivisionId="@emp.Id">Редактировать</a></td>
            </tr>
        }
    </table>
</div>
